extends Resource

"""
A single layer of a `LayerMaterial`

The `properties` `Dictionary` holds a `mask` that is used when blending the layers and can hold a LayerTexture for each channel (for example albedo, height. etc...)
"""

# warning-ignore:unused_class_variable
export var properties : Dictionary
# warning-ignore:unused_class_variable
export var name := "Untitled Layer"
